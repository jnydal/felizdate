from django import forms


class ProfileForm(forms.Form):
    # Placeholder form so legacy imports resolve. Fields can be filled in as needed.
    pass


class AdvancedForm(forms.Form):
    # Placeholder form so legacy imports resolve. Fields can be filled in as needed.
    pass

